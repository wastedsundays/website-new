import Navigation from './Navigation';
import Logo from '../assets/logo';

const Header = () => {
  return (
    <header>
        <Logo />
        <Navigation />
    </header>
  );
}

export default Header;